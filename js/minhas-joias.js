// Função para redirecionar o usuário para a página "meus-dados.html".
const meusDados = () => {
  // Redireciona o usuário para a página "../html/meus-dados.html".
  window.location = "../html/meus-dados.html";
};

// Função para redirecionar o usuário para a página "meus-resgates.html".
const meusResgates = () => {
  // Redireciona o usuário para a página "../html/meus-resgates.html".
  window.location = "../html/meus-resgates.html";
};

// Função para redirecionar o usuário para a página "minhas-joias.html".
const minhasJoias = () => {
  // Redireciona o usuário para a página "../html/minhas-joias.html".
  window.location = "../html/minhas-joias.html";
};

// Função para redirecionar o usuário para a página "index.html".
const sair = () => {
  // Redireciona o usuário para a página "../index.html".
  window.location = "../index.html";
};